[project]
name = "torrent-llm"
version = "0.1.0"
description = "Distributed recursive multi-agent LLM inference with learned low-rank activation compression"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Yuvaraj R." },
    { name = "Haise" },
]

# Deliberately minimal. We are NOT depending on petals: its last release
# (2.2.0.post1, Nov 2023) pins transformers<4.35, which predates Llama-3
# support entirely. See docs/adr/001-minimal-harness.md.
dependencies = [
    "torch>=2.4",
    "transformers>=4.44",
    "accelerate>=0.34",
    "safetensors>=0.4",
    "grpcio>=1.66",
    "protobuf>=5.27",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
# The HTTP layer is instrumentation for our own experiments, not part of the
# inference path, so it stays out of the core dependency set: a shard node
# should not have to install a web framework to host layers.
api = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
]
dev = [
    "pytest>=8.0",
    "grpcio-tools>=1.66",
    "ruff>=0.6",
    "httpx>=0.27",  # FastAPI's TestClient transport
]

[project.scripts]
torrent-shard = "torrent_llm.cli:shard_main"
torrent-run = "torrent_llm.cli:run_main"
torrent-api = "torrent_llm.api.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/torrent_llm"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
